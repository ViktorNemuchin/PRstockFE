import { EventDetailedModel } from './event-detailed-model';

describe('EventDetailedModel', () => {
  it('should create an instance', () => {
    expect(new EventDetailedModel()).toBeTruthy();
  });
});
